version: '3.8'

services:
    auth-service:
        build: .
        ports:
            - '3001:3000'
        environment:
            - NODE_ENV=development
            - PORT=3000
        volumes:
            - .:/app
            - /app/node_modules
